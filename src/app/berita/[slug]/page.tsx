import{notFound}from"next/navigation";import Link from"next/link";import{Header}from"@/components/header";import{Footer}from"@/components/footer";import{news}from"@/lib/data";export function generateStaticParams(){return news.map(n=>({slug:n.slug}))}export default async function News({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const n=news.find(x=>x.slug===slug);if(!n)notFound();return <><Header/><main className="section"><article className="container" style={{maxWidth:760}}><span className="eyebrow">{n.date}</span><h1 className="title">{n.title}</h1><p className="muted" style={{fontSize:'1.1rem'}}>{n.text}</p><p className="muted">Kegiatan ini menjadi bagian dari komitmen sekolah untuk menyediakan pengalaman belajar yang relevan, menantang, dan memberi ruang bagi setiap siswa untuk berkembang.</p><Link className="btn outline" href="/#berita">Kembali ke berita</Link></article></main><Footer/></>}